# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 代码库概览

该代码库似乎是一个纯 CSS 项目，包含一套原子化/功能优先的 CSS 工具类，类似于 Tachyons 或一个迷你的 Tailwind CSS。

- **`css/basic.css`**: 提供了一组基础的 CSS 工具类，用于布局（Flexbox、positioning）、排版（font sizes、weights）、间距（margin、padding）、边框等。
- **`css/tools.css`**: 提供了另一组工具类，与 `basic.css` 中的功能有些重叠，但也有扩展，例如更丰富的 Flexbox 辅助类和文本截断类。

## 开发工作流

此代码库目前没有发现构建、 lint 或测试的命令。开发工作似乎主要是直接编辑 `.css` 文件。
